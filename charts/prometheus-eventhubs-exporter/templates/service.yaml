apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-eventhubs-exporter.fullname" . }}
  labels:
    {{- include "prometheus-eventhubs-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-eventhubs-exporter.selectorLabels" . | nindent 4 }}
