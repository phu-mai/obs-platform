{{- if .Values.agent.flow.deployment.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "grafana-agent.fullname" . }}-flow-deployment
  labels:
    {{- include "grafana-agent.labels" . | nindent 4 }}
    agent-type: deployment
    mode: flow
data:
  {{- range $key, $val := .Values.agent.flow.deployment.secret.content }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
{{- end }}
