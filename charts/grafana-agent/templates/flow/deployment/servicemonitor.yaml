{{- if and .Values.service.flow.deployment.enabled .Values.serviceMonitor.flow.deployment.enabled -}}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "grafana-agent.fullname" . }}-flow-deployment
  labels:
    {{- include "grafana-agent.labels" . | nindent 4 }}
    agent-type: deployment
    mode: flow
    {{- with .Values.serviceMonitor.flow.deployment.additionalLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  endpoints:
  - port: http-metrics
    honorLabels: true
    {{- if .Values.serviceMonitor.flow.deployment.interval }}
    interval: {{ .Values.serviceMonitor.flow.deployment.interval }}
    {{- end }}
    {{- if .Values.serviceMonitor.flow.deployment.metricRelabelings }}
    metricRelabelings:
    {{ tpl (toYaml .Values.serviceMonitor.flow.deployment.metricRelabelings | nindent 6) . }}
    {{- end }}
    {{- if .Values.serviceMonitor.flow.deployment.relabelings }}
    relabelings:
    {{ tpl (toYaml .Values.serviceMonitor.flow.deployment.relabelings | nindent 6) . }}
    {{- end }}
    {{- with .Values.serviceMonitor.flow.deployment.tlsConfig }}
    tlsConfig:
    {{- toYaml . | nindent 6 }}
    {{- end }}
  selector:
    matchLabels:
      {{- include "grafana-agent.selectorLabels" . | nindent 6 }}
      agent-type: deployment
      mode: flow
{{- end }}
