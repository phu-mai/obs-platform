{{- if .Values.agent.static.daemonset.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "grafana-agent.fullname" . }}-static-daemonset
  labels:
    {{- include "grafana-agent.labels" . | nindent 4 }}
    agent-type: daemonset
    mode: static
data:
  {{- range $key, $val := .Values.agent.static.daemonset.secret.content }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
{{- end }}
