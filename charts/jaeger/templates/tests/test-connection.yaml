apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jaeger-standalone.fullname" . }}-test-connection"
  labels:
    {{- include "jaeger-standalone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jaeger-standalone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
